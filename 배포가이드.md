# cnation 유투브 다운로더 배포 가이드

## 1. GitHub에 소스 올리기

1. 제공된 ZIP을 노트북에서 압축 해제합니다.
2. GitHub에서 새 저장소를 만듭니다. 이름 예: `cnation-youtube-downloader`. 개인용이면 Private로 만들어도 됩니다.
3. 압축 안의 **프로젝트 파일 전체**를 저장소에 올립니다. ZIP 자체를 올리는 방식은 사용하지 마세요.
4. GitHub 첫 화면에서 `package.json`, `app`, `lib`, `worker`, `public`, `vercel.json`이 보이는지 확인합니다.

숨김 파일인 `.gitignore`, `.env.example`도 포함해야 합니다. `node_modules`, `.next`, `.env.local`, 실제 비밀번호·토큰은 올리지 않습니다. 제공 ZIP에는 실행에 필요한 소스가 들어 있으며 이 제외 항목들은 담지 않았습니다.

## 2. Vercel에 연결

1. Vercel에서 **Add New → Project**를 선택합니다.
2. 방금 만든 GitHub 저장소를 가져옵니다.
3. Framework Preset은 **Next.js**, Node.js는 **24.x**로 설정합니다.
4. Root Directory는 `package.json`이 있는 위치로 지정합니다. 저장소 최상위에 파일을 올렸다면 기본값을 유지합니다.
5. 기본 설치·빌드 설정을 사용합니다. 직접 입력해야 한다면 설치 `npm ci`, 빌드 `npm run build`입니다.
6. 첫 배포를 실행합니다. 아직 설정이 없으면 앱에 ‘앱 설정을 마무리해 주세요’가 나오는 것이 정상입니다.

## 3. 비공개 영상 저장소 연결

Vercel 프로젝트의 Storage에서 Blob 저장소를 만들거나 연결합니다. **접근 모드는 Private**를 선택합니다. 공개 저장소와 비공개 저장소는 서로 다르므로 처음 생성할 때 확인하세요. 이 앱 전용 저장소를 권장합니다. [Vercel Blob 안내](https://vercel.com/docs/vercel-blob)

연결하면 프로젝트 환경 변수에 `BLOB_READ_WRITE_TOKEN`이 생성되는지 확인합니다. 화면에서 선택한 배포 환경에 연결해야 합니다. 처음에는 Production에 연결하고, Preview에서도 시험하려면 Preview에도 연결합니다.

## 4. 접속 코드와 비밀 값 설정

노트북에서 프로젝트 폴더를 열고 다음을 실행합니다.

```sh
npm run secrets
```

화면에 나온 세 값을 Vercel 프로젝트 **Settings → Environment Variables**에 각각 추가합니다.

| 이름 | 용도 |
| --- | --- |
| `APP_ACCESS_CODE` | 앱에서 사용자가 입력할 접속 코드. 12자 이상 |
| `APP_SECRET` | 세션과 작업 증명 서명용 값. 32자 이상 |
| `CRON_SECRET` | 자동 파일 정리 작업 인증용 값 |
| `BLOB_READ_WRITE_TOKEN` | 앞 단계에서 저장소 연결 시 생성되는 값 |

접속 코드는 비밀번호 관리 앱 등에 따로 보관합니다. 나머지 값은 앱 화면에 입력하지 않습니다. 변수 이름에 `NEXT_PUBLIC_`를 붙이지 마세요. 이 값들은 모두 서버 전용입니다.

Vercel 배포에서는 Sandbox가 프로젝트의 OIDC 인증을 자동 사용합니다. 먼저 프로젝트·팀에서 Sandbox 사용이 가능한지 확인하세요. 정상적인 Vercel 배포에는 개인용 Vercel API 토큰을 따로 넣을 필요가 없습니다. [Sandbox 인증 안내](https://vercel.com/docs/sandbox)

## 5. 다시 배포

환경 변수와 저장소 연결을 마친 뒤 **Deployments → 최신 배포 → Redeploy**를 실행합니다. 설정을 바꾸기 전에 만들어진 배포에는 새 값이 반영되지 않습니다.

발급된 HTTPS 주소를 열었을 때 ‘개인용 앱 접속 코드’가 보이면 접속 코드를 넣고 잠금을 해제합니다. 이어서 직접 소유하거나 다운로드 허가를 받은 짧은 공개 영상으로 낮은 화질부터 시험합니다.

화질 선택 → 동영상 준비하기 → 파일로 다운로드 순서로 확인합니다. 서버에서 처음 처리 도구를 준비할 때 몇 분이 걸릴 수 있습니다. 작업하는 동안 앱을 열어 두는 편이 좋습니다. 모바일 브라우저가 백그라운드로 가면 화면 갱신이 멈출 수 있으며, 돌아오면 작업을 다시 확인합니다.

## 6. 스마트폰에 저장

### 아이폰

Safari에서 주소를 엽니다. 사진에 보관하려면 ‘사진 앱·갤러리용’을 선택합니다. 완료 후 ‘사진 앱·갤러리로 저장 준비’를 누르고, 휴대폰으로 파일을 가져온 뒤 ‘공유 메뉴 열기’를 누릅니다. 공유 메뉴에 ‘비디오 저장’이 제공되면 선택합니다.

해당 메뉴가 없거나 100 MB가 넘으면 ‘파일로 다운로드’를 사용합니다. 파일 앱의 다운로드 폴더에서 MP4를 열고 공유 메뉴에서 ‘비디오 저장’을 선택할 수 있는지 확인합니다. 메뉴 제공 여부는 iOS 버전과 영상 형식·기기에 따라 다릅니다. [Apple Safari 사용 안내](https://support.apple.com/ko-kr/guide/iphone/iph1fbef4daa/ios)

Safari 공유 메뉴의 ‘홈 화면에 추가’를 선택하면 앱처럼 실행할 수 있습니다. 설치 이름은 **cnation 유투브 다운로더**이며, 아이콘 아래 긴 이름은 iOS가 줄여 표시할 수 있습니다. 웹앱 실행 시 상단 브랜드와 제목에는 지정한 이름이 들어 있습니다.

### 안드로이드

Chrome에서 주소를 엽니다. 완료 후 ‘파일로 다운로드’를 선택합니다. 일반적으로 파일 앱의 Download 폴더에 저장되며, 사진·갤러리 앱에 나타나는지는 제조사와 미디어 검색 방식에 따라 다릅니다. 필요하면 파일 앱에서 영상을 Movies 폴더로 옮기거나 공유 기능을 이용하세요.

Chrome 메뉴의 ‘앱 설치’ 또는 ‘홈 화면에 추가’를 이용할 수 있습니다. 사진·갤러리 호환성을 원하면 MP4 사진 앱용 옵션을 선택하세요. MKV는 파일 보관용이며 별도 재생 앱이 필요할 수 있습니다.

## 7. 보관 기간·사용량

완료 파일은 24시간 동안 받을 수 있습니다. ‘서버 파일 삭제하고 새 영상’을 누르면 서버 작업을 중지하고 그 작업의 임시 파일을 삭제합니다. 이미 받은 휴대폰 파일은 삭제하지 않습니다.

`vercel.json`에 매일 UTC 03:00 정리 작업이 들어 있습니다. 업로드 25시간이 지난 파일을 정리하므로 정상적인 실행에서는 약 25~49시간 사이에 실제 삭제됩니다. Vercel의 Cron 실행 기록도 확인하세요. Cron이 실패하면 파일이 더 오래 남을 수 있습니다.

Sandbox 실행 시간, 임시 파일 용량, 다운로드 트래픽이 Vercel 사용량에 포함됩니다. 무료 무제한 서비스가 아닙니다. 현재 플랜의 한도와 지출 알림을 확인하세요. [Vercel 요금](https://vercel.com/pricing)

접속 코드는 개인용 접근 제한입니다. 공개 다중 사용자 서비스용 계정·이용량 관리·사용자별 할당량은 포함하지 않았습니다. 여러 탭에서 동시에 영상 확인을 누르면 각각 서버 작업이 생길 수 있습니다.

## 문제가 생겼을 때

| 증상 | 확인할 내용 |
| --- | --- |
| 설정을 마무리하라는 안내가 계속 보임 | 변수 철자·길이, Private Blob 연결, 적용 환경, Redeploy 여부 |
| 영상 확인 단계에서 서버 연결 오류 | Vercel Runtime Logs, Sandbox 권한·사용 한도, Blob 연결 |
| ‘추가 인증’ 또는 요청 제한 메시지 | YouTube가 서버 IP를 차단했을 수 있음. 다른 공개 영상이나 나중에 재시도 |
| 사진 앱에 저장 메뉴가 없음 | 사진 앱용 MP4 선택 여부. 파일 다운로드 후 공유 메뉴 사용 |
| 긴 영상·고화질 영상 실패 | 30분·500 MB·최대 처리 시간 제한. 낮은 화질 선택 |
| 작업 만료 | 영상 확인부터 다시 시작. 화질 선택 대기는 5분 |
| 예전 앱 이름이 남아 있음 | 새 배포 확인 후 브라우저 새로고침. 기존 홈 화면 아이콘을 제거하고 다시 추가 |

유료·비공개·로그인 필요 영상이나 DRM 보호 영상을 우회하는 기능은 없습니다. 배포가 정상이어도 모든 YouTube 영상의 다운로드를 보장하지 않습니다.

## 개발 환경에서 실제 처리 연결하기

Vercel CLI를 사용하는 개발 환경이라면 `vercel link`로 프로젝트를 연결하고 `vercel env pull .env.local`로 개발용 환경 변수를 받습니다. OIDC 토큰은 수명이 짧으므로 만료되면 다시 받습니다. Production만 설정했다면 Development에도 필요한 변수와 Blob 연결을 적용해야 합니다.

Vercel 외부 환경에서 꼭 필요한 경우에만 `.env.example`의 `VERCEL_TOKEN`, `VERCEL_TEAM_ID`, `VERCEL_PROJECT_ID` 세 값을 함께 사용합니다. 토큰은 서버 환경에만 두고 소스에 포함하지 않습니다.
